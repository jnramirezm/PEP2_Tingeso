spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      uri: http://config-service:8081
eureka:
  instance:
    hostname: 10.106.55.50
    prefer-ip-address: true