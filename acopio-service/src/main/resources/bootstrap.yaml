spring:
  application:
    name: acopio-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/milkstgo
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create-drop